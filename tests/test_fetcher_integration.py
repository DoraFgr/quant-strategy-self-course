"""Light, non-network test placeholder.

This file used to contain an integration test that required network access. The
project policy is to avoid integration tests, so we keep a tiny local unit test
here to ensure the tests folder contains at least one simple test (optional).
"""

def test_placeholder():
    """A minimal test that always passes (no network).

    Replace or expand this test with lightweight unit tests that don't require
    external services.
    """
    assert True
